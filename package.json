{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @microfront/* --parallel start",
    "build": "lerna run --scope @microfront/* build",
    "serve": "lerna run --scope @microfront/* --parallel serve",
    "clean": "lerna run --scope @microfront/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "workspaces": {
    "packages": [
      "shell",
      "mfe1",
      "mfe2"
    ]
  }
}
